/* =========================================================
   PACIFIC EDUCATION
   PRICING BROKEN-LINE TEST
   TEST FILE ONLY — NOT PRODUCTION
   ========================================================= */

(function () {
    "use strict";

    const TEST_VERSION = "1.0.0";

    // INTENTIONALLY BROKEN LINE — TEST ONLY
    const brokenLine = ;

    window.PacificEducationPricingBrokenLineTest = {
        version: TEST_VERSION,
        status: "BROKEN_TEST_FILE"
    };

})();
